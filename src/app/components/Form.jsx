const Form = () => {
  return (
    <Form onclick>
      <label htmlFor="todo">
        <input
          type="text"
          name="todo"
          id="todo"
          placeholder="Write your nxt task"
        />
      </label>

      <button>
        <span>Submit</span>
        <svg>
          <path d="" />
        </svg>
      </button>
    </Form>
  );
};

export default Form;
